# Changesets

Use `npm run changeset` to record a user-facing change before merging to `master`.

Use `npm run version-packages` to apply pending changesets locally.

Use `npm run release` to publish the package to npm after versions have been updated.
